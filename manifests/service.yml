apiVersion: v1
kind: Service
metadata:
    name: theharpretsinghdockerwithcicd
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: theharpretsinghdockerwithcicd